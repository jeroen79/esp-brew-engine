export interface IClientSettings {
  beepEnabled: boolean,
  beepVolume: number,
  speechEnabled: boolean,
  speechVolume: number,
  speechRate: number,
  voiceUri: string | null,
  darkMode: boolean,
}
